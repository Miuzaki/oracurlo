services:
  oraculoaviator:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4005:4005"
    environment:
      - NODE_ENV=production
      - PORT=4005
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
